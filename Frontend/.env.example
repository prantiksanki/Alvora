# Copy this to .env and fill in your values

# Backend URL — set to your deployed backend for production builds
# Leave empty to use Vite's dev proxy (localhost development)
VITE_API_URL=https://your-backend.onrender.com
